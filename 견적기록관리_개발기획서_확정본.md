# 견적/가격 기록 관리 기능 — 개발 기획서 (확정본 rev.3)

> MiniERP2 (.NET 10 / WinForms / SQLite)
> 근거: `theborges4skin-code/MiniERP2` main 코드 실측 (rev.2: 2026-07-20, rev.3: 2026-07-20 같은 날 재검증)
> 상태: **Step 1~4 구현 완료(2026-07-20).** 5~14는 여전히 설계 단계, 착수는 별도 지시. 명시적 지시 없는 코드 자동생성 없음.
> rev.3 변경사항: rev.2 검토 중 실측이 어긋난 부분을 코드+라이브 DB 직접 대조로 정정(§1.1/§3.3/§3.4/§6.3/§7.3/Step 11).
> 핵심 정정: `PurchaseSkuTable`/`PurchaseSkuPriceHistory`/`ChannelSkuFieldHistory`/`SalesChannelTable.IsPurchase,IsSales`는
> **이미 2026-07-15 세션에서 구현되어 있었다**(rev.2는 "신규 생성"으로 잘못 서술). `ChannelSkuPriceHistoryForm`은
> 삭제되고 `ChannelSkuHistoryForm`(가격+필드 이력 통합)으로 교체됨. `OutboundRepository`의 P3 대상 메서드는
> `UpdateOutbound`가 아니라 `UpdateDetail`.

---

## 0. 목적

원단·원부재로 변형된 매입가/메일가의 정규화된 견적 이력을, **문서를 먼저 만들지 않고 DB에 기록**한다.
언제·어떤 방식으로 전달했는지(또는 전달하지 않았는지)를 함께 남겨 견적/매입가를 프로그램에서 통합관리하고,
이후 저장된 견적을 편집해 **견적서·가격조정 문서로 출력**한다.
추가로 OFS 매핑으로 발생한 신규 CSKU의 실제 출고 실적을 견적 관리 화면에서 함께 조회한다(§7).

---

## 1. 현행 실측

### 1.1 존재하는 자산

| 자산 | 위치 | 내용 |
|---|---|---|
| `ChannelSkuTable` | `Database/DbSchema.cs:34` | PK(`ChannelCode`,`CskuCode`) + `Msku`, `SupplyPrice`, `InvoiceDisplayName`, `Unit`, `Packing` |
| `ChannelSkuPriceHistory` | `DbSchema.cs:43` | `Id, ChannelCode, Msku*, OldPrice, NewPrice, ChangedAt, Reason, Note` (*실제 저장값은 CskuCode) |
| `ChannelSkuFieldHistory` | `DbSchema.cs:52` | CSKU의 매칭 마스터SKU·송장표시명·비고 등 **필드** 변경이력(가격이력과 분리, 2026-07-15 신설) |
| `ChannelSkuRepository` | `Database/ChannelSkuRepository.cs` | `Upsert`/`Delete`/`GetByChannelAndCskuCode`/`GetAllByMsku`/`GetAllByChannel`/`GetAll`/`GetPriceHistory`/`CreateIfNew` |
| `ChannelSkuHistoryForm` | `Forms/ChannelSkuHistoryForm.cs` | 가격이력(`ChannelSkuPriceHistory`)+필드이력(`ChannelSkuFieldHistory`)을 시간순으로 합쳐 보여주는 단일 CSKU 이력 조회 화면(`CSkuForm.cs`에서 호출). **구 `ChannelSkuPriceHistoryForm`은 삭제됨(2026-07-15)** |
| `ItemTable.CostPrice` + `ItemCostHistory` | `DbSchema.cs:16,26` | **Msku 단일 원가** + 변경로그 |
| `PurchaseSkuTable` | `DbSchema.cs:63` | ✅ **이미 존재(2026-07-15).** PK(`ChannelCode`,`Msku`) + `PurchasePrice`, `Unit`(기본 kg), `Note`, `UpdatedAt` — 매입처(채널)별 마스터SKU 매입가. `IsPrimary` 컬럼은 없음(§3.3 참고) |
| `PurchaseSkuPriceHistory` | `DbSchema.cs:73` | ✅ **이미 존재.** `Id, ChannelCode, Msku, OldPrice, NewPrice, ChangedAt, Reason, Note`. `QuoteId`는 없음(§3.4에서 추가) |
| `PurchaseSkuRepository` | `Database/PurchaseSkuRepository.cs` | ✅ 이미 존재. `Upsert`(가격변동 시에만 이력기록)/`Delete`/`GetByChannelAndMsku`/`GetAllByMsku`/`GetAllByChannel`/`GetAll`/`GetPriceHistory` |
| `SalesChannelTable.IsPurchase`/`IsSales` | `DbSchema.cs:484-485` | ✅ **이미 존재(2026-07-15).** 매입처/판매처 겸용 채널 플래그(기존 채널은 IsSales=1 기본) |
| `OutboundDetailTable` | `DbSchema.cs:124` | `ChannelCode, OrderNo, ShipmentGroupKey, TrackingNo, MskuCode, Qty, SupplyPrice, CreatedAt, Status, ConfirmedAt, Recipient, Address, ProductName` + B2B 확장(`PurchaseChannelCode`,`PurchasePrice`,`WeightKg`, 2026-07-15) |
| `OutboundRepository` | `Database/OutboundRepository.cs` | `SaveOutbound`(UPSERT, 충돌감지 포함) / `UpdateDetail`(단건 수정 — **문서 구판이 "UpdateOutbound"로 잘못 인용했던 그 메서드**) / 조회 |
| `OutboundShipmentTable`/`Repository` | `DbSchema.cs:86` | ✅ 이미 존재(2026-07-15). 발송헤더(합포장 그룹) 단위 실운임(`FreightCost`) |
| `SkuMapper` | `Mapping/SkuMapper.cs` | 생성 시 `GetAllByChannel()`의 CskuCode 키 딕셔너리로 캐싱 |
| 문서 계층 | `DbSchema.cs:294~359` | `DocFavoritePhraseTable`, `DocPartyTable`, `DocStatementTable`, `DocStatementLineTable`, `DocHistoryTable` |

`OutboundDetailTable.Status` 실사용 값: `발주확정` / `출고확정` / `출고확정`

### 1.2 공백 및 결함

| # | 사실 | 해석 |
|---|---|---|
| G1 | 매입가에 채널(매입처) 축은 이제 있음(`PurchaseSkuTable`) — 다만 `ItemTable.CostPrice`는 여전히 Msku당 단일값이고 매입처 변경과 자동 동기화되지 않음 | §3.3, [[b2b_purchase_quote_infra_status]] |
| G2 | 이력에 재전송·메모·전용일·전달수단 컬럼 없음 | §3.1, §3.2 |
| G3 | `Upsert`가 `existing is not null && 가격변동`일 때만 기록 → **신규 CSKU/매입SKU 최초 가격 미기록**(`ChannelSkuRepository`/`PurchaseSkuRepository` 둘 다 동일 패턴) | §7.2, §9 단계11 |
| G4 | 이력 수정/삭제 API 없음(조회 전용) | 견적 계층에서 해결 |
| G5 | `ChannelSkuPriceHistory.Msku` 컬럼에 CskuCode 저장(레거시) | rename 안 함(D6). 신규 쿼리 작성 시 주의 |
| G6 | `Models/ChannelSkuPriceHistory.cs`는 dead 파일(`...HistoryModel.cs`가 실사용) | §9 단계11 삭제 |
| G7 | "현재가 + 변경로그" 구조 → 미래 시작 단가·효력기간 표현 불가 | §4 |
| G8 | 견적서·가격조정서는 **데이터 테이블 부재**. `DocHistoryTable`은 파일경로만 기록 | §3.1, §3.2 |
| G9 | **`OutboundDetailTable`에 `CskuCode` 없음(`MskuCode`만)** → 출고 이력을 CSKU 단위로 후처리 불가 | §4.3 |
| **P1** | **`SaveOutbound`의 `ON CONFLICT DO UPDATE SET SupplyPrice = excluded.SupplyPrice`(`OutboundRepository.cs:64`)** — 단가 변경 후 같은 `(ShipmentGroupKey, MskuCode)`로 재저장하면 **과거 출고 건 단가가 현재가로 덮어써짐** | §7.3 — ✅ **2026-07-20 수정 완료** |
| **P2** | 단가 스냅샷 기준이 **출고확정 시점이 아니라 저장 시점**(`OfsForm.cs` 저장 경로) → 7/19 발주·7/22 확정이면 옛 단가가 붙음 | §7.3 |
| **P3** | `UpdateDetail`(`OutboundRepository.cs:146`, 구판 문서에서 `UpdateOutbound`로 오기재)는 `SupplyPrice`를 무조건 갱신 — `OutboundHistoryForm`의 편집 가능한 "납품가" 셀이 이 경로를 그대로 탐 | §7.3 — ✅ **2026-07-20 수정 완료** |
| **P4** | `SkuMapper`가 생성 시 `GetAllByChannel()`을 캐시(`SkuMapper.cs:42-44`) → OFS 화면을 연 채 견적이 반영되면 **화면 표시 단가가 stale** | §7.4 |

### 1.3 설계 판단
기존 `ChannelSkuPriceHistory`/`PurchaseSkuPriceHistory`는 `Upsert` 부산물로 생성되는 **감사로그**다. "이번 달 단가표를 카톡으로 전달"처럼 1건 = N품목인 업무 단위를 담는 구조가 아니다.
→ **견적 헤더/라인 계층을 신설**하고, 하단 저장 시 기존 `Upsert`를 경유시켜 감사로그 불변식을 그대로 보존한다.

---

## 2. 확정 결정

| # | 항목 | 결정 |
|---|---|---|
| D1 | 복수 매입처 | **존재함(빈도 낮음)** — `PurchaseSkuTable` 계열로 이미 지원(2026-07-15). `IsPrimary`(대표 매입처)만 §3.3에서 추가 |
| D2 | 견적번호 | 자동생성번 `Q{yyMMdd}{2자리seq}` + 수동수정 허용(UNIQUE 검사) |
| D3 | 견적일/적용일 | **분리 필수.** 소급(7/20 견적 → 7/1 적용)·미래(→ 8/1 적용) 양방향. 적용일 겹치는 견적끼리는 **건별 선택 후 취급** |
| D4 | 수정/삭제 | 출고 이력 **없으면** 자유로운 라인 수정·삭제 허용. **1건 이상 있으면** 삭제 금지 + **개정 견적** |
| D5 | 단위 | EA/kg **혼용**. 라인별 지정, CSKU 기본값 있음(`ChannelSkuTable.Unit`, 2026-07-15부터 존재) |
| D6 | `Msku` 컬럼 rename | 하지 않음(데이터 이관 리스크 회피) |
| D7 | 견적양식 | **2종.** 기본형(품목·단위·단가) / 상세형(품목·수량·단가·공급가·세액·합계금액) |
| R1 | 대표 매입처 | **직접 가정.** `IsPrimary` 미지정 상태로 반영 시도하면 경고 후 차단 |
| R2 | 소급 시 과거 출고건 단가 갱신 | 제공하되 **기본 OFF**, 확인 2단계 |
| R3 | 세액 반올림 | **라인별 세액원 단위 반올림**, 합계는 라인 합산 |
| R4 | 렌더러 프리셋 | 기존 3프리셋에서 **본 2종(UnitOnly/WithQty)으로 통합** |
| R5 | `EffectiveTo` | **자동 만료.** 같은 채널·CSKU에 다음 Applied 견적 `EffectiveFrom - 1일` |
| R6 | `OutboundDetailTable.CskuCode` 과거분 백필 | **안 함.** 채널+Msku fallback 후속으로 처리 |
| **S1** | OFS 매핑 연계 방식 | **실적 조회 뷰(§7.1) + 자동 Draft 신설(§7.2) 둘 다.** 자동 Draft는 **채널별 ON/OFF** |
| **S2** | P1 스킬 방지 | **본 개발 범위에 포함, 2026-07-20 완료.** 상세 §7.3 |
| **S3** | 단가 스냅샷 기준(P2) | **저장 시점 유지.** 출고확정 시 재평가하지 않음(마감 데이터를 흔듦). 대신 실적 뷰에서 **불일치 경고 표시** — 미착수(§7.1) |
| **S4** | 실적 뷰 기간 축 | **`ConfirmedAt`(출고확정일) 기준.** 미확정 건은 별도 구분 표시 — 미착수 |

---

## 3. 데이터 모델

> 신규 2테이블(§3.1/§3.2, 2026-07-20 구현 완료) + 기존 테이블 `EnsureColumn` 확장 4건(2026-07-20 구현 완료).
> 추가 컬럼 전부 nullable/DEFAULT → 기존 DB 호환.

### 3.1 신규 `PriceQuoteTable` (견적 1건 = 1회 전달 단위) — ✅ 구현 완료

```
Id              INTEGER PRIMARY KEY AUTOINCREMENT
QuoteNo         TEXT NOT NULL DEFAULT ''         -- 자동생성번 + 수동수정 (UNIQUE)
ChannelCode     TEXT NOT NULL                    -- 거래처(매출) 또는 매입처
PriceKind       TEXT NOT NULL DEFAULT 'Supply'   -- Supply(납품) / Purchase(매입)
QuoteFormType   TEXT NOT NULL DEFAULT 'UnitOnly' -- UnitOnly(기본형) / WithQty(상세형)
Origin          TEXT NOT NULL DEFAULT 'Manual'   -- Manual / OfsMapping / Import   ←S1
Title           TEXT NOT NULL DEFAULT ''
QuoteDate       TEXT NOT NULL DEFAULT ''         -- 견적 작성/전달 기준일
EffectiveFrom   TEXT NOT NULL DEFAULT ''         -- 적용 시작일 (필수)
EffectiveTo     TEXT                             -- NULL = 무기한, 다음 견적이 있으면 만료(R5)
AutoApply       INTEGER NOT NULL DEFAULT 0       -- 적용일 되는 즉시 자동반영 여부
Status          TEXT NOT NULL DEFAULT 'Draft'
                -- Draft / Sent / Scheduled / Applied / Superseded / Rejected / Void
DeliveryMethod  TEXT NOT NULL DEFAULT ''         -- 메일/카톡/전화/대면/문서발송/미전달
DeliveredAt     TEXT
DeliveredTo     TEXT NOT NULL DEFAULT ''
Note            TEXT NOT NULL DEFAULT ''
PriceBasis      TEXT NOT NULL DEFAULT 'VatExcl'  -- VatExcl / VatIncl
RootQuoteId     INTEGER                          -- 리비전 체인 뿌리(원본이면 NULL)
RevisionNo      INTEGER NOT NULL DEFAULT 0       -- 0=원본, 1,2,… 개정
SupersededBy    INTEGER                          -- 이 건을 덮은 견적 Id
RevisionReason  TEXT NOT NULL DEFAULT ''
CreatedAt       TEXT NOT NULL DEFAULT ''
UpdatedAt       TEXT NOT NULL DEFAULT ''
```

인덱스
```
IX_PriceQuote_Channel_Eff   (ChannelCode, EffectiveFrom)
IX_PriceQuote_Revision      (RootQuoteId, RevisionNo)
IX_PriceQuote_Origin        (ChannelCode, Origin, Status)   ←자동 Draft 조회용
UX_PriceQuote_QuoteNo       UNIQUE (QuoteNo)
```

최신본 판정은 컬럼이 아니라 `SupersededBy IS NULL`로 한다.

### 3.2 신규 `PriceQuoteLineTable` — ✅ 구현 완료

```
Id            INTEGER PRIMARY KEY AUTOINCREMENT
QuoteId       INTEGER NOT NULL
RowNo         INTEGER NOT NULL DEFAULT 0
CskuCode      TEXT NOT NULL DEFAULT ''    -- PriceKind=Supply일 때 필수
Msku          TEXT NOT NULL DEFAULT ''    -- 항상 채움(매입/집계 기준축)
ItemNameSnap  TEXT NOT NULL DEFAULT ''    -- 전달 당시 품명 스냅샷
Spec          TEXT NOT NULL DEFAULT ''
Unit          TEXT NOT NULL DEFAULT 'EA'  -- EA / kg / BOX …
Qty           REAL NOT NULL DEFAULT 0     -- WithQty에서만 유효
OldPrice      REAL                        -- 직전 유효가 (NULL = 신규품목)
NewPrice      REAL NOT NULL DEFAULT 0
SupplyAmount  REAL NOT NULL DEFAULT 0     -- 계산 저장
Tax           REAL NOT NULL DEFAULT 0     -- 계산 저장
Total         REAL NOT NULL DEFAULT 0     -- 계산 저장
ChangeReason  TEXT NOT NULL DEFAULT ''    -- 원가상승/환율/물동조정/신규
Note          TEXT NOT NULL DEFAULT ''
IsApplied     INTEGER NOT NULL DEFAULT 0
PromotedFrom  INTEGER                     -- 자동 Draft 라인에서 승격된 경우 원본 라인 Id ←S1
```

인덱스: `(QuoteId, RowNo)`, `(CskuCode)`, `(Msku)`

**파생값(`SupplyAmount`/`Tax`/`Total`)을 컬럼으로 저장하는 이유:** 문서 재출력 시 당시 계산결과가 그대로 재현되어야 한다. 이후 반올림 규칙이 바뀌어도 과거 문서는 불변.

### 3.3 `PurchaseSkuTable` / `PurchaseSkuPriceHistory` 확장 — ⚠️ rev.2에서 "신규 생성"으로 잘못 서술, **실제로는 기존 테이블에 컬럼만 추가**

```
PurchaseSkuTable에 EnsureColumn 추가:
  IsPrimary     INTEGER NOT NULL DEFAULT 0 -- 대표 매입처 (Msku당 최대 1건)

PurchaseSkuPriceHistory에 EnsureColumn 추가:
  QuoteId       INTEGER  -- 견적 발신과 매입가 변경이력을 연결
```

**복수 매입처 처리 (D1, R1)**
- `ItemTable.CostPrice`는 **폐기하지 않고 대표원가/fallback으로 유지** — 기존 마진분석·정산 로직 무손상.
- `IsPrimary=1` 매입처 가격 반영 시 `ItemTable.CostPrice`를 동기화(기존 `ItemCostHistory`가 이미 기록됨).
- `IsPrimary` **미지정 상태에서 매입 견적 반영을 시도하면 경고 후 차단**(R1).
- 별도 `VendorTable`은 만들지 않는다. `SalesChannelTable.IsPurchase`(이미 존재) 플래그로 처리.

### 3.4 기존 테이블 확장 (`EnsureColumn`) — ✅ 구현 완료

| 테이블 | 추가 컬럼 | 목적 | 상태 |
|---|---|---|---|
| `ChannelSkuPriceHistory` | `QuoteId INTEGER` | 견적 → 이력 역추적 (`Reason`은 2026-07-15부터 이미 존재, 재추가 안 함) | ✅ |
| `PurchaseSkuTable` | `IsPrimary INTEGER DEFAULT 0` | 대표 매입처 (§3.3) | ✅ |
| `PurchaseSkuPriceHistory` | `QuoteId INTEGER` | 견적 → 매입 이력 역추적 (§3.3) | ✅ |
| `SalesChannelTable` | `AutoQuoteDraft INTEGER DEFAULT 0` | 채널별 자동 Draft 생성 ON/OFF (S1) | ✅ |
| `DocHistoryTable` | `SourceQuoteId INTEGER` | 발행문서 → 견적 연결 | ✅ |
| `OutboundDetailTable` | `CskuCode TEXT DEFAULT ''` | G9 해소(§4.3, §7.1) | ✅ |
| ~~`SalesChannelTable`~~ | ~~`IsPurchase INTEGER DEFAULT 0`~~ | ~~매입처 플래그~~ | ❌ **이미 존재(2026-07-15) — 재추가 불필요, rev.2 오류** |

---

## 4. 상태 · 적용일 · 리비전

*(rev.2와 동일 — §4.1~4.3, 코드 변경 없는 순수 설계 절이라 재검증 불필요. 착수 시 이 문서의 §4 그대로 참고)*

---

## 5. 견적양식 2종 (D7)

*(rev.2와 동일)*

---

## 6. UI

### 6.1 신규 `PriceQuoteForm` — 탭 2개

*(rev.2와 동일 — 아직 미착수)*

### 6.2 실적 기준 탭 — §7.1

### 6.3 기존 폼 확장
- `CSkuForm` → `매입가(대표)` · `마진` 열 추가. 우클릭 → "이 CSKU의 견적 이력".
- `ChannelSkuHistoryForm`(구 `ChannelSkuPriceHistoryForm` — **2026-07-15에 이미 교체됨**) → `Reason` · `견적번호` 열 추가(가격이력 행에만 표시, 필드이력 행은 해당 없음으로 공란).
- `ChannelConfigForm` → 채널별 `AutoQuoteDraft` 체크박스(S1).
- `MainHub` → `견적·단가 관리` 진입 버튼 신설.

---

## 7. OFS 매핑 연계 — 신규

### 7.0 현행 동작 (실측)

```csharp
// OfsForm.cs (2026-07-20 기준, 이전 세션 변경으로 줄번호는 계속 움직임 — 메서드명 기준으로 찾을 것)
var csku = _channelSkuRepository.GetByChannelAndCskuCode(order.ChannelCode, order.MappedSku);
...
SupplyPrice = csku?.SupplyPrice ?? 0m,
```

OFS는 저장 시점에 `ChannelSkuTable`을 **직접 조회해 단가를 스냅샷**한다.
→ **견적 반영이 `SupplyPrice`를 갱신하면, 그 이후 OFS 저장 건은 자동으로 새 단가를 쓴다. 추가 개발 불필요.**
→ 7/20부터 적용된 견적가는 7/20 이후 저장 건에만 붙고, 7/1~7/19 건은 옛 단가로 남아 **기간별 단가가 자연 분리**된다.

단, §1.2의 P1~P4가 이 자연 분리를 깨뜨린다. P1/P3는 §7.3에서 처리 완료. P2/P4는 아래 §7.3~7.4 참고(미착수).

### 7.1 실적 기준 탭 (A채널 7월 판매품목·단가 조회) — 미착수

*(rev.2와 동일한 설계 — `OutboundDetailTable.CskuCode` 컬럼은 이제 존재(§3.4 완료)하므로, 착수 시 §4.3의 백필 없이 신규분부터 바로 채워진다)*

### 7.2 자동 Draft 신설 — 미착수

*(rev.2와 동일)*

### 7.3 단가 스냅샷 오염 방지 (P1~P3) — S2 → **P1/P3 구현 완료(2026-07-20)**

| # | 위치 | 조치 | 상태 |
|---|---|---|---|
| P1 | `OutboundRepository.cs` `SaveOutbound` UPSERT | `SET SupplyPrice = CASE WHEN OutboundDetailTable.Status = '출고확정' THEN OutboundDetailTable.SupplyPrice ELSE excluded.SupplyPrice END` — **출고확정 건은 단가 불변** | ✅ 완료 |
| P3 | `OutboundRepository.cs` `UpdateDetail`(구판 문서에서 `UpdateOutbound`로 오기재) | 동일 가드 적용(단, 일반 UPDATE라 `excluded.` 불필요 — 갱신 전 행의 `Status`/`SupplyPrice`를 그대로 참조). 출고확정 건의 단가 변경 시도는 저장되지 않고, `OutboundHistoryForm`이 "N건은 납품가 변경이 적용되지 않았습니다" 안내를 표시하도록 `UpdateDetail`이 잠김 여부(bool)를 반환하게 함 — §4.2 ② 소급 경로(미착수)로만 정식 허용 예정 | ✅ 완료 |
| P2 | `OfsForm.cs` 스냅샷 저장 | **저장 시점 유지(S3).** 재평가하지 않음. 대신 §7.1 실적 뷰에서 ⚠ 경고 컬럼으로 노출 | 미착수 |

P1/P3 가드가 없으면 §4.2 ②(소급 갱신)뿐 아니라 **평범한 재저장/재실행만으로도 과거 단가가 바뀐다.** 견적 기능 신뢰성의 전제조건이라 최우선 처리했다.

`Tests/OutboundRepositoryTests.cs`에 회귀 테스트 추가: 이미 `출고확정`인 행을 다른 `SupplyPrice`로 재저장/재수정해도 `SupplyPrice`가 그대로인지, `발주확정` 상태에서는 여전히 자유롭게 갱신되는지 검증.

### 7.4 캐시 무효화 (P4) — 미착수

*(rev.2와 동일)*

### 7.5 질문 3건에 대한 답 (요약)

*(rev.2와 동일)*

---

## 8. 문서 연계 (2단계) — 미착수

*(rev.2와 동일)*

---

## 9. 개발 순서

| 단계 | 내용 | 선행 | 상태 |
|---|---|---|---|
| 1 | `DbSchema`: `PriceQuoteTable` · `PriceQuoteLineTable` 신설 + `EnsureColumn` 4건(§3.4, `IsPurchase`는 제외) | – | ✅ **완료(2026-07-20)** |
| 2 | **P1/P3 스킬 방지 가드**(§7.3) — `SaveOutbound` / `UpdateDetail` | 1 | ✅ **완료(2026-07-20)** |
| 3 | 모델 2종 + `PriceQuoteRepository` (CRUD · 목록 · 채번 · 출고이력 판정) | 1 | ✅ **완료(2026-07-20)** |
| 4 | `PriceQuoteForm` 골격 — 탭 구조 / 필터 / 목록 / 헤더 편집 | 3 | ✅ **완료(2026-07-20)** |
| 5 | 라인 그리드 + `CskuPickerDialog` 연동 + `OldPrice` 자동채움 + 양식별 열 토글(§5) | 4 | 대기 |
| 6 | 상태 전이 · 전달기록 검증 · `Scheduled` 기동 스캔 | 5 | 대기 |
| 7 | **단가 반영** — `ChannelSkuRepository.Upsert` 경유 + `QuoteId` 백필 + 소급 경고(§4.2) | 6, 2 | 대기 |
| 8 | `OutboundDetailTable.CskuCode` 기록(`OfsForm` 저장부, §3.4에서 컬럼은 이미 생김) + **실적 기준 탭**(§7.1) | 1 | 대기 |
| 9 | 리비전(개정 / 삭제금지) + 출고 이력 판정 연결(§4.3) | 7, 8 | 대기 |
| 10 | **자동 Draft 신설 + 승격**(§7.2) + `ChannelConfigForm` 토글 | 7 | 대기 |
| 11 | `PurchaseSkuTable`/`PurchaseSkuPriceHistory` **확장**(`IsPrimary`/`QuoteId` 컬럼만, §3.3 — 테이블 자체는 이미 있음) + 매입 견적 경로 + `IsPrimary` → `ItemTable.CostPrice` 동기화 | 7 | 대기(스키마 부분은 Step1에 선반영됨) |
| 12 | `CSkuForm` · `ChannelSkuHistoryForm` 확장, 역조회(구 폼명 `ChannelSkuPriceHistoryForm` 아님 — 이미 교체됨) | 7 | 대기 |
| 13 | G3 수정(신규 CSKU 최초가 이력 기록), G6 dead 파일 삭제 | 독립 | 대기 |
| 14 | 문서 출력 연계 → 렌더러 DTO 매핑 + `DocHistoryTable.SourceQuoteId` | 7, 렌더러 완성 | 대기 |

- **1~4번은 이미 완료.** 다음 최우선은 5번(라인 그리드 + `CskuPickerDialog` 연동)부터 — 지금은
  헤더만 저장되고 라인이 없는 견적만 만들 수 있다(라인 있는 견적의 헤더 재저장/삭제는 Step 4에서
  일부러 막아둠, PriceQuoteForm.cs 주석 참고).
- **1~9가 요구사항 본체.** 10(자동 Draft) · 11(매입) · 14(문서)은 분리 착수 가능.
- 13은 독립 → 아무 때나.

---

## 10. 리스크

| 리스크 | 내용 | 상태 |
|---|---|---|
| **과거 단가 스킬** | P1/P3 — 재저장·수정만으로 출고확정 건 단가가 바뀜. **이미 발생 스킬 가능성** | ✅ 2026-07-20 가드 적용 완료. 적용 전 기존 데이터 영향 범위는 별도 조사 안 함(과거 손상분 복구는 이번 범위 밖) |
| 출고 이력 소급판정 | G9 fallback 구간에서 과거 "없음" 판정 | 보수적 판단 유지. `CskuCode` 컬럼은 생겼으나(§3.4) 과거분 백필은 안 함(R6) |
| 스냅샷·적용일 경계불일치 | P2 — 7/19 저장·7/22 확정 건은 옛 단가 | S3에 따라 재평가 안 함. §7.1 ⚠ 경고로 가시화(미착수) |
| 소급 갱신 사고 | §4.2 ②를 망각된 기간에 실행 | 기본 OFF + 확인 2단계 + Note 자동 기록(미착수) |
| 자동 Draft 비대화 | 매핑이 잦은 채널에서 Draft 라인 폭증 | 채널별 ON/OFF(S1) + 중복 방지 + 승격 시 원본 제거(미착수) |
| 양식 이원화 | 견적양식이 본 기획과 렌더러 양쪽에 존재 | R4로 통합. 렌더러 프리셋 정의를 본 문서 §5로 단일화(미착수) |
| `ItemTable.CostPrice` 이중 소스 | 대표 매입처 가격과 어긋될 수 있음 | `IsPrimary` 반영 시 항상 동기화. 미지정이면 반영 차단(R1, §9 단계11 미착수) |
