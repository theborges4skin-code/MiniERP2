# 매핑 시스템 통합 개편 개발기획서

작성일: 2026-08-03
대상: OFS(`Forms/OfsForm.cs`), 마감/이익분석(`Services/ClosingOrchestrator.cs`, `Forms/SettlementForm.cs`, `Forms/MonthlyClosingForm.cs`)의 SKU 매핑 전 과정
상태: Phase 1~3 완료. Phase 4 중 "진입점 교체"까지 완료(2026-08-03) — OFS/SettlementForm/UnmappedQueueForm이 이제 전부 `MappingWorkbenchDialog`를 연다. "구 편집기 제거"(OrderSkuMappingDialog.cs 삭제)는 사용자 요청으로 보류 — 실제 화면 검증 후 별도 진행. Phase 5 중 "회귀 테스트 정비"까지 완료(OrderLoader Revenue 읽기, ClosingRunRepository Quantity/SampleRevenue 왕복 테스트 추가; 전체 404/406 통과, 나머지 2건은 무관한 기존 이슈). **"실사용 검증"은 아직 안 됨 — WinForms GUI라 코드로는 검증 불가, 사용자가 직접 세 진입점(OFS/SettlementForm/미매핑큐)을 클릭해봐야 함.**

---

## 1. 배경 및 목적

OFS(발주처리)와 마감/이익분석(정산분석)이 서로 다른 매핑 화면/입력방식을 쓰다 보니 관리 필드가 들쭉날쭉하고, 오매핑 위험이 있다는 문제 제기. 특히:

- OFS: 발주 건수가 적어 사용자가 눈으로 보고 판단 가능
- 마감/이익분석: 수천 건을 일일이 볼 수 없어 **오매핑 방지가 훨씬 중요**

→ 마스터 DB를 중심에 두고, CSKU가 같으면 두 화면의 매핑 조건을 하나로 모은다.

---

## 2. 현황 진단 (AS-IS, 코드 확인 사실)

### 2.1 데이터/엔진 레이어는 이미 통합되어 있음

OFS와 마감/이익분석은 이미 **동일한 엔진과 동일한 테이블**을 씁니다.

- 양쪽 다 `Mapping/SkuMapper.cs` → `Database/MappingRepository.cs`를 통해 `RuleExact`/`RuleTemp`/`RuleException`/`RuleCondition`+`RuleConditionDetail`을 읽고 씁니다.
- OFS: `Forms/OfsForm.cs`가 `new SkuMapper(...)`로 각 주문 항목에 `ApplyMapping()` 호출.
- 마감: `Services/ClosingOrchestrator.cs` → `DataLoaders/SettlementLoader.cs`가 행 단위로 동일한 `SkuMapper.ApplyMapping()`을 호출.
- 즉 **한쪽에서 등록한 규칙이 다른 쪽에 즉시 반영됨** — "마스터 DB 중심" 요구사항은 데이터 레벨에서는 이미 충족.

### 2.2 진짜 문제: 매핑 편집기가 3원화되어 있음

| 편집기 | 쓰이는 곳 | 1:1 정확매핑 등록 | 조건부매핑 등록 | 마스터SKU/CSKU 신규등록 |
|---|---|:---:|:---:|:---:|
| `Forms/OrderSkuMappingDialog.cs` | OFS 개별 미매핑 클릭 | ✅ | ❌ | ✅ |
| `Controls/QuickMappingPanel.cs` | OFS·`SettlementForm` 인라인 패널 | ❌(조건부만 생성) | ✅ | ❌ |
| `Forms/UnmappedQueueForm.cs` | 마감(월마감) 대량 미매핑 그리드 | ✅ | ❌ | ❌ |

창마다 할 수 있는 일이 다르기 때문에, **어디서 등록하느냐에 따라 매핑의 성격(정확/조건부/신규등록 가능여부)이 달라짐**. 이것이 "필드가 다양해서 데이터 관리에 오점이 많다"의 실체.

### 2.3 1:1 매핑 키가 요청 조건과 다름 + 가격 데이터 자체가 없음

- 현재 exact 매칭 키는 `상품명+옵션명` 두 필드뿐 (`Mapping/SkuMapper.cs`). 수량·가격은 매칭 키에 없음.
- `Models/OfsOrderItem.cs`(OFS 발주 항목)에는 **가격 필드 자체가 없음**. `DataLoaders/OrderLoader.cs`도 상품명/옵션명/수량/수취인/연락처/주소만 읽음.
- `Forms/ChannelConfigForm.cs`의 발주서 매핑 표준필드 목록(`OrderMappingFields`)에도 `Revenue`(매출액)가 **빠져 있음** → 애초에 채널 설정 화면에서 발주서의 가격 컬럼을 매핑할 방법이 없음.
- 정산/마감 쪽은 `Models/SettlementData.cs`에 `Revenue` 필드가 있지만, **DB(`SettlementData` 테이블)에는 저장되지 않고 메모리 전용**이며, 미매핑 집계 테이블 `ClosingUnmapped`도 상품명+옵션명 2필드 키(`SourceKey`)로만 그룹핑되어 가격/수량 구분 없이 뭉뚱그려집니다.

즉 "판매가격을 1:1 매핑 조건에 포함"은 UI 문구 하나 바꾸는 문제가 아니라, **OFS 발주서 로딩 파이프라인 + 마감 집계 파이프라인에 가격 데이터 자체를 새로 흘려보내야** 가능합니다.

---

## 3. 확정된 정책 결정 (사용자 확인 완료)

| 쟁점 | 결정 |
|---|---|
| "판매가격"의 정의 | **매출액(Revenue, 고객이 실제 지불하는 판매가)** 기준. 정산액(Settlement, 채널이 셀러에게 지급하는 금액)과는 별개 개념이므로 사용 안 함 |
| OFS 발주서에 가격 컬럼이 없는 채널 처리 | 가격이 없으면 **1:1 정확매핑 비활성, 조건부매핑으로만** 처리. 채널설정에서 가격 컬럼을 나중에 매핑하면 그때부터 1:1 사용 가능 |
| 기존 RuleExact(2필드 키, 수량/가격 정보 없음) 처리 | **레거시로 그대로 유지**, 신규 등록분부터만 4필드 키 적용 (신구 병행) |

---

## 4. 개편 설계 (TO-BE)

### 4.1 1:1 정확매핑 확장 — 상품명+옵션명+수량+매출액(판매가) 완전일치

- `RuleExact`/`RuleTemp`에 `Quantity`(nullable int), `Price`(nullable decimal, = Revenue) 컬럼 추가.
  - **두 컬럼 모두 NULL** → 기존 방식 그대로(상품명+옵션명만 비교) = **레거시 규칙**.
  - **두 컬럼 모두 채워짐** → 상품명+옵션명+수량+판매가 4개 완전일치해야만 매칭 = **신규 규칙**.
- 매칭 우선순위: 같은 상품명+옵션명 키에 대해 (a) 4필드 신규 규칙 중 수량·가격까지 일치하는 것이 있으면 최우선 적용 → (b) 없으면 레거시(2필드) 규칙으로 폴백 → (c) 그래도 없으면 조건부매핑으로 이동. 옵션을 가격으로 구분하는 케이스(상품명+옵션명은 같은데 가격 차이로 실제 다른 옵션인 경우)를 정확히 가려내면서, 레거시 규칙이 갑자기 안 먹는 회귀를 방지.
- 가격 데이터가 없는 채널(OFS 발주서에 가격 컬럼 미매핑)의 주문 항목은 애초에 4필드 후보가 될 수 없으므로 자동으로 레거시/조건부 경로로만 처리됨 — 정책 결정 그대로 반영.

### 4.2 조건부매핑 — 현행 유지

`RuleCondition`/`RuleConditionDetail`/`Mapping/ConditionEvaluator.cs` 변경 없음. 통합 매핑창 안에 현재 `MappingForm`의 "조건부 매핑(상세)" 탭과 `QuickMappingPanel`이 만드는 방식을 그대로 흡수.

### 4.3 매핑제외 — 현행 유지

`RuleException` + `[EXCLUDED]` 센티널 그대로. 4필드 확장 대상에서 명시적으로 제외(요청사항).

### 4.4 마스터DB 등록 / CSKU 신규생성 — 현행 유지

`OrderSkuMappingDialog`의 `ItemRepository.Upsert` → `ChannelSkuRepository.CreateIfNew` → `MappingRepository.UpsertExactRule` 흐름을 그대로 통합창에 포함. (신규 등록 시 1:1 정확매핑을 택하면 자동으로 수량+가격 조건까지 함께 저장되도록 UI에서 유도.)

### 4.5 통합 "매핑하기" 창 신설

`OrderSkuMappingDialog` + `QuickMappingPanel` + `UnmappedQueueForm`의 기능을 하나의 신규 모달(가칭 `MappingWorkbenchDialog`)로 통합. 어디서 열든 동일한 4가지 액션(1:1 정확매핑 등록/조건부매핑 등록/매핑제외/마스터DB 신규등록)을 제공.

**진입 지점(모두 이 새 창을 여는 것으로 통일):**
1. OFS 그리드에서 미매핑 행 더블클릭/우클릭 → 새 창 (건별, 현재 `OrderSkuMappingDialog` 대체)
2. OFS/`SettlementForm`의 인라인 `QuickMappingPanel` → 패널은 유지하되, "정식 매핑" 버튼이 같은 새 창을 엶 (또는 패널 자체를 새 창의 임베디드 모드로 재사용)
3. 마감(`MonthlyClosingForm`)의 미매핑 리스트(`UnmappedQueueForm`) → 행 선택 후 새 창 (대량 처리이므로 리스트→선택→창 열기→다음 항목 반복 흐름 유지, 다만 창 자체는 공용)

**창 내부 구성(안):**
- 상단: 대상 정보 표시(채널/상품명/옵션명/수량/매출액 — 값이 없으면 "가격정보없음" 표시)
- 탭 또는 섹션: [1:1 정확매핑 등록] (가격 있을 때만 활성화) / [조건부매핑 등록] / [매핑제외] / [마스터DB 신규등록(CSKU 생성)]
- 기존 각 편집기의 검증/미리보기 로직(실시간 매칭예상 미리보기 등)은 그대로 이식

---

## 5. 스키마 변경안

```sql
ALTER TABLE RuleExact ADD COLUMN Quantity INTEGER NULL;
ALTER TABLE RuleExact ADD COLUMN Price    REAL    NULL;   -- Revenue(매출액) 기준
ALTER TABLE RuleTemp  ADD COLUMN Quantity INTEGER NULL;
ALTER TABLE RuleTemp  ADD COLUMN Price    REAL    NULL;
-- RuleException은 변경 없음 (요청대로 현행 유지)

ALTER TABLE SettlementData ADD COLUMN Revenue REAL NOT NULL DEFAULT 0; -- 현재 메모리 전용 → 영속화
ALTER TABLE ClosingUnmapped ADD COLUMN Quantity INTEGER NULL;
ALTER TABLE ClosingUnmapped ADD COLUMN SampleRevenue REAL NULL;
```

`ClosingUnmapped.SourceKey`의 집계 기준도 손봐야 함: 가격 매핑이 있는 채널은 상품명+옵션명+수량+매출액 4필드로 집계 키를 만들어 서로 다른 가격의 옵션이 한 미매핑 행으로 뭉치지 않게 함. 가격 매핑이 없는 채널은 기존 2필드 키 유지.

---

## 6. 파이프라인 변경 상세

### 6.1 OFS 측
1. `Forms/ChannelConfigForm.cs`의 `OrderMappingFields` 목록에 `StdField.Revenue` 추가(발주서에 매출액/판매가 컬럼이 있는 채널만 매핑하면 됨, 필수 아님).
2. `Models/OfsOrderItem.cs`에 `decimal? Revenue` 속성 추가.
3. `DataLoaders/OrderLoader.cs`가 매핑되어 있으면 `StdField.Revenue` 컬럼을 읽어 채움(없으면 null 유지 → 1:1 비활성 채널로 자연스럽게 처리됨).
4. `Mapping/SkuMapper.ApplyMapping`이 `Quantity`+`Revenue`를 함께 받아 4필드 키 매칭 로직 수행.

### 6.2 마감/정산 측
1. `SettlementData` 테이블에 `Revenue` 영속화(현재 메모리 전용 → DB 저장).
2. `DataLoaders/SettlementLoader.cs`의 `ApplyMappingAndProfit`이 현재 매칭용 synthetic `OfsOrderItem`을 만들 때 `Qty`/`Revenue`를 드롭하고 있음(확인됨) → 이걸 채우도록 수정.
3. `ClosingUnmapped` 집계 시 4필드 키 사용(가격 매핑 채널 한정).
4. `Forms/SettlementForm.cs`의 `OnOpenMappingHelperFromSettlementRowClick`(현재 `Revenue`를 synthetic item에 안 넘기고 있음, 확인됨)도 동일하게 수정.

---

## 7. 마이그레이션 / 하위호환

- 기존 `RuleExact`/`RuleTemp` 데이터는 `Quantity`/`Price` NULL 상태로 그대로 유지(레거시 규칙으로 계속 동작). 별도 마이그레이션 스크립트 불필요.
- 신규 등록 UI(통합 매핑창)에서 1:1 정확매핑을 만들 때만 4필드 값을 채워 저장.
- `MappingHistory` 감사로그에 신규/레거시 구분 표시 여부는 구현 시 결정.

---

## 8. 영향범위 / 리스크

- **범위 밖**: 광고비 매핑 엔진(`AdRule*`/`AdMappingEngine`/`AdMappingRepository`)은 SKU 매핑과 구조는 비슷하지만 완전히 별도 시스템 — 이번 개편 대상 아님.
- **리스크 1**: 통합 매핑창이 기존 3개 편집기의 세부 동작(실시간 미리보기, 셀클릭 조건 자동주입 등)을 빠짐없이 이식해야 함 — 회귀 테스트 필요.
- **리스크 2**: `ClosingUnmapped` 집계 키 변경 시 가격 매핑 채널의 미매핑 리스트 행 수가 늘어날 수 있음(같은 상품명+옵션명이라도 가격별로 별도 행) — 의도된 정밀화지만 사용자 체감 UX 확인 필요.
- **리스크 3**: `SettlementData.Revenue` 영속화는 스키마 변경 + 기존 저장된 행에는 소급 적용 불가(과거 데이터는 Revenue=0으로 남음, 재매칭 대상 아니므로 무해).

---

## 9. 단계별 진행 계획(안)

| Phase | 내용 |
|---|---|
| Phase 1 | 스키마 확장(`RuleExact`/`RuleTemp`/`SettlementData`/`ClosingUnmapped`) + `OfsOrderItem.Revenue` + `ChannelConfigForm` 발주서 매핑 필드에 Revenue 추가 |
| Phase 2 | `SkuMapper` 4필드 매칭 로직(우선순위: 신규4필드 → 레거시2필드 → 조건부) + `OrderLoader`/`SettlementLoader`/`SettlementForm` 파이프라인에 Quantity/Revenue 결선 |
| Phase 3 | 통합 "매핑하기" 창(`MappingWorkbenchDialog`) 신규 구현 — 기존 3개 편집기 로직 이식 |
| Phase 4 | OFS·`SettlementForm`·`UnmappedQueueForm`의 진입점을 전부 새 창 호출로 교체, 기존 `OrderSkuMappingDialog`/구식 `QuickMappingPanel` 등록경로/구 `UnmappedQueueForm` 편집 UI 제거 |
| Phase 5 | 회귀 테스트(`Tests/`) 정비 + 실사용 검증 |

---

## 10. 남은 확인사항

- 통합 매핑창을 완전히 새 클래스로 만들지, `QuickMappingPanel`을 확장해 재사용할지는 Phase 3 착수 시 코드 레벨에서 다시 판단 필요(현재는 설계 방향만 확정).
- `ClosingUnmapped`의 "가격 매핑 채널 여부" 판단 기준(채널설정에 Revenue 컬럼이 매핑되어 있는지)을 어디서 조회할지는 Phase 2에서 `ChannelConfig.OrderFieldMappings.ContainsKey(StdField.Revenue)`로 확인 예정.
