# 채널설정 → 엑셀 다운로드 / 엑셀 일괄 등록 기능개선 기획서

작성일: 2026-07-31 (검토 반영: 2026-07-31 2차)
대상 화면: 채널 설정 창 (`Forms/ChannelConfigForm.cs`)
상태: 기획(완료, 2차 검토 반영) → 코드 작성 단계

---

## 1. 배경 및 목적

현재 채널은 `AddChannelDialog`로 **1건씩만** 추가 가능함. 입력 가능한 값은 채널명 + "설정 복사(기존 채널 선택)" 두 개뿐이고, 나머지 설정(채널유형/환율/발주서 매핑/정산서 매핑/거래처 정보)은 추가 후 탭을 옮겨 다니며 수기로 채워야 함.

채널 수가 늘어나는 상황(거래처 채널, B2B 매입처, 풀필먼트 신규 가상채널 등)에서 반복 수기 입력 비용이 큼.

→ **엑셀(오프라인) 다운로드 → 오프라인 기입 → 일괄 업로드**로 N개 채널을 한 번에 등록/수정할 수 있게 함.

---

## 2. 현행 구조 확인 (코드 기준 사실)

### 2.1 채널 데이터는 3곳에 나뉘어 저장됨 → 이 기능의 핵심 제약

| 구분 | 저장소 | 접근 경로 | 비고 |
|---|---|---|---|
| 채널 목록(마스터) | SQLite `SalesChannelTable` | `SalesChannelRepository.Upsert/Delete` | `ChannelCode, ChannelName, GroupName, IsFavorite, DisplayOrder, LastUsedDate, IsPurchase, IsSales` |
| 채널 설정 | JSON 파일 `channels_config.json` | `ChannelConfigService.Load()/Save(List<ChannelConfig>)` | **리스트 전체를 통째로 덮어쓰는 구조** |
| 거래처 정보(공급받는자) | SQLite `DocParty` | `DocPartyRepository.Save/GetByChannelCode` | 문서(거래명세서/견적) 발행용. 등록번호/상호/대표자/주소/업태/종목/전화/이메일 |

즉 채널 1건 등록에 **DB 2회 쓰기 + JSON 파일 1회 전체 저장**이 필요함. 단일 SQLite 트랜잭션으로 전부 묶을 수 없음 → 4.5절 롤백 설계가 필요한 이유.

### 2.2 채널 코드는 사용자 입력 대상이 아님

`Utils/ChannelCodeGenerator.GenerateNext()`가 기존 코드 중 `CH(\d+)` 패턴의 최댓값 +1로 `CH001` 형식을 자동 생성함. 일괄등록에서도 신규 행의 **채널코드를 공란**으로 두는 것이 원칙(2.4절 아래 상세).

### 2.3 신규 채널 생성 시 실제 실행 흐름 (`ChannelConfigForm` 1435~1469행)

1. `ChannelCodeGenerator.GenerateNext()`로 코드 생성
2. `SalesChannelRepository.Upsert(new SalesChannel{...})`
3. (복사 선택 시) 원본 `ChannelConfig`를 JSON 직렬화 왕복으로 깊은 복사 → 코드/이름 교체 → 리스트에 add → `ChannelConfigService.Save()`
4. `LoadData()` → 트리 갱신

일괄 등록은 **이 흐름을 N회 반복**하는 것이 기본. 신규 로직 최소화.

### 2.4 채널 유형별로 매핑 대상 표준필드가 다름

`ChannelConfigForm`의 정적 배열 기준(사실):

- **발주서 매핑**(`OrderMappingFields`, 채널유형 무관 11개):
  `ProductNo, ProductName, OptionName, Quantity, Recipient, Phone, Address, DeliveryMessage, OrderDate, Remark, ChannelHint`
- **정산서 매핑** → 채널유형에 따라 3세트로 분기:
  - 기본: `ProductName, OptionName, Quantity, Revenue, ShippingFee, SettlementAmount, TrackingNo`
  - 쿠팡그로스: `ProductName, OptionName, Quantity, Revenue, SettlementAmount, ShippingFee, HandlingFee`
  - 쿠팡로켓: `ProductName, OptionName, Quantity, Revenue, SettlementAmount, TrackingNo, TaxNo`

→ 업로드 검증 시 **채널유형과 정산서 매핑 필드의 적합성 체크**가 필요함(유형에 없는 필드가 기입되면 경고).

### 2.5 필드 매핑 1건의 구성 (`Models/FieldMapping.cs`)

`SheetName`(공란=첫 시트), `HeaderRow`(기본 1), `Column`(열 이름 또는 주소), `FixedValue`(있으면 미탐색, 항상 이 값 사용).

### 2.6 채널유형 이거저 (`Models/ChannelType.cs`)

`General(온라인), CoupangGeneral(쿠팡 일반), CoupangRocket(쿠팡 로켓), ElevenStreet(11번가), CoupangGrowth(쿠팡 그로스), AmazonUs(아마존 미국), AmazonJp(아마존 일본), Partner(거래처), B2B(B2B 거래처), Other(기타)`

업로드에서는 **한글 라벨**(`ToKoreanLabel()` 결과)을 노출하고, 파싱 시 라벨→enum 역변환함.

### 2.7 재사용 가능한 기존 자산

| 자산 | 위치 | 이 기능에서의 용도 |
|---|---|---|
| `ExcelFileOpener` | Utils | 시트 여러 포함 열기 |
| `ExportHelper.SaveExcel` / `ShowPostExportDialog` | Utils | 엑셀 다운로드 후 "파일 열기/폴더 열기/닫기" 공통 처리 |
| `MasterSkuImportMappingDialog` | Forms | 엑셀 가져오기 다이얼로그의 기존 UX 패턴 참고 |
| `MetaSheetHelper` | Utils | `_META` 시트 읽기/쓰기 패턴 참고(양식 버전 표기) |
| `DbBackupService.CreateBackup/Restore` | Database | 커밋 전 DB 스냅샷 |
| `ExcelLikeDataGridView` | Controls | 미리보기 그리드 |

---

## 3. 기능 범위

### 3.1 포함 (v1)

1. **엑셀 다운로드** → 빈 템플릿 xlsx 저장
2. **현재 설정 내보내기** → 등록된 전체 채널의 값을 엑셀양식으로 채워서 저장(라이트립 편집용)
3. **엑셀 불러오기 → 미리보기/분류 → 일괄 반영** → 신규/수정/오류 분류 후 사용자 확인 시 커밋

### 3.2 v1 제외 (사전 명시)

| 항목 | 제외 사유 |
|---|---|
| 보조 소스(`GrowthAuxSources`) | 채널별 N개 중첩 구조 + 쿠팡그로스 전용. 시트 복잡도 대비 대상 채널 1~2개 |
| 광고비 헤더(`AdFileLayouts`) | 한 채널의 복수 레이아웃 중첩 구조 |
| 택배사 출력 고정값(`CourierHeaderOverrides`) | 택배사 마스터 의존 |
| CFS 설정(`GrowthCfsFee`) | 쿠팡그로스 전용 단일 채널 |
| 채널 삭제 | 파괴적 액션. 업로드에 없는 채널은 **무시**(삭제하지 않음) |

→ 제외 항목은 기존 방식대로: 일괄 등록으로 껍데기를 만든 뒤, "설정 복사" 또는 해당 탭에서 개별 편집.

---

## 4. 상세 설계

### 4.1 엑셀 구조 (다중 시트)

시트 간 **조인 키는 `채널명`**(코드가 아직 없는 신규 행 때문). 따라서 파일 내 채널명 중복은 오류 처리.

#### 시트 `_META` (숨김 또는 최우측)

`schema_version`, `exported_at`, `app` → 양식 버전 불일치 시 업로드 거부용. `MetaSheetHelper`의 key/value 패턴 준용.

#### 시트 1 — `채널` (1행 = 1채널)

| 열 | 대응 필드 | 필수 | 기본값 / 규칙 |
|---|---|---|---|
| 채널코드 | `SalesChannel.ChannelCode` | 아니오 | 공란=신규(자동생성), 기입=기존 채널 수정 |
| 채널명 | `ChannelName` | **예** | 조인 키. 파일 내 중복 금지 |
| 채널유형 | `ChannelConfig.ChannelType` | 예 | 한글 라벨 드롭다운(2.6절) |
| 그룹 | `SalesChannel.GroupName` | 아니오 | 공란 허용 |
| 표시순서 | `DisplayOrder` | 아니오 | 공란=0 |
| 즐겨찾기 | `IsFavorite` | 아니오 | Y/N, 공란=N |
| 매입 | `IsPurchase` | 아니오 | Y/N, 공란=N |
| 매출 | `IsSales` | 아니오 | Y/N, **공란=Y**(모델 기본값과 일치) |
| 환율 | `ExchangeRate` | 아니오 | 공란=1 |
| 누적발주서 | `IsCumulativeOrderFile` | 아니오 | Y/N, 공란=N |
| 누적조회일수 | `CumulativeOrderWindowDays` | 아니오 | 공란=5 |
| 자동발주채널힌트 | `AutoOrderHints` | 아니오 | 쉼표 구분 |
| 설정복사원본 | (업로드 전용) | 아니오 | 기존 채널명 기입 시 그 채널 설정을 깊은 복사한 뒤 이 행의 값으로 덮어씀. 상세 규칙은 4.1.1절 |

> `IsAutoOrderStandardPreset`은 **전체 채널 중 1개만 허용**되는 배타 플래그라 업로드 대상에서 제외함(중복 생성 위험). 기존 방식대로 화면에서 지정. 단, "설정복사원본"이 이 플래그를 가진 채널일 경우의 처리는 4.1.1절 참고.

##### 4.1.1 `설정복사원본` 적용 규칙 (확정)

v1에서 제외한 4개 항목(보조소스 / 광고비 레이아웃 / 택배사 출력 고정값 / CFS)을 일괄등록으로 채울 수 있는 **유일한 경로**가 이 복사임(`ChannelConfigForm.CopyChannelConfig`가 `ChannelConfig` 전체를 JSON 직렬화 왕복으로 깊은 복사함). 따라서 이전에 정했던 충돌 규칙을 아래로 고정함.

**적용 순서 (1행당)**

1. `설정복사원본` 지정 시 원본 `ChannelConfig`를 깊은 복사 → 채널코드/채널명만 이 행의 값으로 교체
2. `채널` 시트의 기본정보 열(채널유형·환율·누적발주서 등)로 **덮어쓰기**. 공란인 열은 복사본 값을 유지
3. 매핑 시트 반영(아래 규칙)
4. **`IsAutoOrderStandardPreset` 리셋**: 원본 채널의 `IsAutoOrderStandardPreset`이 `true`이면, 복사본 생성 시 이 값을 **무조건 `false`로 리셋**하고 미리보기 화면에 경고 표시(`"○○ 채널을 복사 원본으로 사용했지만 자동발주(표준) 프리셋 플래그는 복제하지 않았습니다"`). 이 플래그는 전체 채널 중 정확히 1개만 허용되는데, 현재 `CopyChannelConfig`/`AutoOrderChannelResolver`(`FirstOrDefault`로만 조회) 어디에도 유일성을 강제하는 코드가 없어서, 복사로 여러 채널이 `true`가 되면 자동발주 파싱이 조용히 엉뚱한 채널로 튈 수 있음. 일괄등록은 같은 원본을 여러 행에서 반복 지정하기 쉬워 위험이 커지므로 리셋을 기본 동작으로 고정.

**매핑 반영 단위 — 매핑 종류별 전면 교체**

| 상황 | 동작 |
|---|---|
| 해당 채널의 `발주서매핑` 행이 1건 이상 존재 | 발주서 매핑 **전체를 시트 내용으로 교체**(복사본/기존 값 폐기) |
| 해당 채널의 `발주서매핑` 행이 0건 | 복사본(신규) 또는 기존 값(수정) 그대로 유지 |
| 정산서매핑 | 위와 동일하게 독립 판단 |

필드 단위 부분 덮어쓰기를 하지 않는 이유: 복사본에 남은 원본 매핑과 시트 값이 섞이면 최종 상태를 사용자가 예측할 수 없고, "지우기"를 표현할 방법이 없음.

**복사원본 제약**

- 원본으로 지정 가능한 대상: 기존 등록 채널 + **같은 파일 내 다른 신규 행**
- 단, **1단계만 허용** → 복사본으로 만들어진 행을 다시 원본으로 지정하는 연쇄 복사는 오류
- 자기 자신 지정, 상호 참조(A→B→A)는 오류
- 같은 파일 내 후행 행을 원본으로 쓰는 경우 처리 순서는 원본 → 대상 순으로 정렬해 반영

#### 시트 2 — `발주서매핑` (1행 = 채널 × 표준필드)

| 열 | 내용 |
|---|---|
| 채널명 | 시트1과 조인 |
| 표준필드 | 한글 라벨(발주서 11필드) |
| 시트이름 | 공란=첫 시트 |
| 헤더행 | 공란=1 |
| 열이름 | |
| 고정값 | 기입 시 열이름 무시(로드 동작과 동일) |

#### 시트 3 — `정산서매핑`

시트 2와 동일 구조. 검증 시 채널유형별 허용 필드(2.4절)와 대조.

#### 시트 4 — `거래처정보`

| 열 | 대응 (`DocParty`) |
|---|---|
| 채널명 | 조인 키 |
| 등록번호 / 상호 / 대표자 / 주소 / 업태 / 종목 / 전화 / 이메일 | RegNo / CompanyName / CeoName / Address / BizType / BizItem / Tel / Email |

시트에 없는 `DocParty.ProfileName`은 저장 시 **채널명과 동일하게 자동 세팅**한다(현재 화면 저장 로직 `ChannelConfigForm.cs:335`와 동일 동작이며, 시트에는 별도 열을 두지 않음).

행이 공란이면 해당 행 무시(현재 화면 저장 로직의 처리와 동일).

#### 시트 5 — `참고(유효값)`

채널유형 라벨 목록, 발주서/정산서 표준필드 라벨 목록, Y/N 규칙. 시트1·2·3의 해당 열에는 이 시트를 원본으로 하는 **데이터 유효성 검사 드롭다운**을 걸어 오타를 입력 시점에 차단(EPPlus 지원 범위).

### 4.2 진입점 (UI)

채널 설정 창 좌측 툴바(기존 [추가]/[삭제] 옆)에 버튼 3개 추가:

- `[엑셀 다운로드]` → 빈 템플릿 저장 → `ExportHelper.ShowPostExportDialog`
- `[현재 설정 내보내기]` → 전체 채널의 값을 양식으로 채워 저장
- `[엑셀 일괄 등록]` → 파일 선택 → 미리보기 다이얼로그

### 4.3 미리보기 / 분류 단계 (신규 다이얼로그)

파일을 읽어 즉시 반영하지 않고 **분류 결과를 보여준 뒤 사용자 확인**을 받음.

행 상태 분류:

| 상태 | 판정 조건 | 커밋 동작 |
|---|---|---|
| 신규 | 채널코드 공란 + 채널명이 기존에 없음 | 코드 생성 후 신규 등록 |
| 수정 | 채널코드 기입(존재) 또는 채널명 일치 | 기입된 값만 갱신 |
| 오류 | 4.4절 검증 실패 | 커밋 제외 |
| 무시 | 행자체 공란 등 비행 | 스킵 |

하단 요약: `신규 N건 / 수정 N건 / 오류 N건 / 변경없음 N건`.

**오류 행 처리 정책(확정): 전체 차단.** 오류 행이 1건이라도 있으면 커밋 버튼을 비활성화하고, 사용자가 파일을 고쳐 다시 올리게 함. 부분 반영 옵션을 제공하지 않음 → 어디까지 반영됐는지 추적이 어렵고, 재업로드 시 중복 반영이 복잡해지기 때문.

그리드는 `ExcelLikeDataGridView` 사용, 오류 행 배경 강조 + 사유 열 표시.

### 4.4 검증 규칙

**파일 단위**
1. `_META.schema_version`이 앱이 아는 버전인지
2. 필수 시트(`채널`) 존재 여부
3. `채널` 시트 내 채널명 중복 없음

**행 단위**
4. 채널명 공란 → 오류
5. 채널유형이 알려진 라벨인지
6. 채널코드가 기입인데 DB에 없음 → 오류(오타 방지. 신규 코드 생성 금지)
7. 채널명이 기존과 같은데 채널코드는 다른 기존 코드 → 오류(모호)
7-1. **(수정 — 최초 초안 오류 정정) 채널코드가 공란인 행의 채널명이 기존 DB 채널명과 일치하는 경우는 "오류"가 아니라 §4.3의 정상 분류 경로다.** §4.3은 이미 "채널코드 기입 또는 채널명 일치 → 수정"으로 정의하므로, 코드 없이 이름만으로 기존 채널에 매칭되는 것은 의도된 동작(코드를 몰라도 이름만으로 수정 가능하게 하려는 목적)이며 이를 오류로 막으면 §4.3과 모순된다. 대신 실제 위험(오타로 엉뚱한 기존 채널을 덮어씀)은 **경고**로 완화한다: 채널코드 공란 + 이름 매칭으로 기존 채널에 매칭된 행은 미리보기에 "이 행은 채널코드 없이 이름만으로 기존 채널 '{채널코드} - {채널명}'을 찾아 수정합니다"를 항상 표시해 사용자가 의도한 채널이 맞는지 확인하게 한다(차단하지 않음). `SalesChannelTable`은 `ChannelCode`만 PK이고 `ChannelName`엔 유니크 제약이 없어 이런 오타 위험 자체는 실재하므로(`CskuPickerDialog`/`MonthlyClosingForm`/`PriceQuoteForm` 등이 채널명을 사실상의 키로 조회), 경고 노출은 유지한다.
8. 환율/표시순서/누적조회일수 숫자 파싱 실패 → 오류
9. `설정복사원본`이 기존 채널명도, 이 파일의 다른 신규 행도 아님 → 오류
9-1. `설정복사원본`이 자기 자신이거나 상호 참조 → 오류
9-2. `설정복사원본`으로 지정된 행이 그 자체로 복사본(연쇄 복사) → 오류
10. 매핑 시트의 `채널명`이 시트1이나 DB에 없음 → 오류
11. 표준필드 라벨이 해당 채널의 유형 허용 목록에 없음 → **경고**(커밋은 허용하되 그 매핑 행만 제외)
12. 헤더행이 1 미만 → 오류

**정합성 경고(차단 아님)**
13. 누적발주서 Y인데 발주서매핑에 `발주일` 없음 → 경고(`ChannelConfig` 설명의 "발주일을 매핑하지 않으면 동작하지 않음"이 명시됨)
14. 채널유형이 마켓플레이스 이외인데(`Partner/B2B/Other`) 정산서 매핑이 기입됨 → 경고
15. `설정복사원본` 채널의 `IsAutoOrderStandardPreset`이 `true` → **경고**(차단 아님). 4.1.1절 규칙대로 복사본은 자동으로 `false`로 리셋되며, 이 경고는 "리셋됐다"는 사실을 사용자에게 알리기 위함.

### 4.5 커밋 및 롤백 설계 (2.1절 제약 대응)

저장소가 3곳이라 단일 트랜잭션 불가 → **선(先) 스냅샷, 실패 시 전체 되돌리기** 방식.

커밋 순서:

1. `DbBackupService.CreateBackup("채널 일괄 등록 전")` → SQLite 스냅샷
2. `channels_config.json`을 같은 백업 폴더로 복사(타임스탬프 부여)
3. **메모리에서 전체 결과 조립** → `List<ChannelConfig>` 전체와 `SalesChannel` 목록, `DocParty` 목록을 최종 형태로 만든 뒤
4. SQLite 쓰기(채널 Upsert + DocParty Save)를 **하나의 SQLite 트랜잭션**으로 실행
5. 성공 시 `ChannelConfigService.Save(전체 리스트)` 1회 호출(파일 통째 저장)
6. 5단계 실패 시 → 4단계 트랜잭션은 이미 커밋되었으므로 **1·2의 스냅샷으로 복원한 후 사용자 안내**

> JSON 저장이 마지막인 이유: 파일 저장이 DB 트랜잭션보다 실패 확률이 낮고(파일 잠금/권한), 실패 시 되돌릴 대상이 파일 하나로 좁아짐.

부분 실패 상황에서 화면 상태와 저장소 상태가 어긋나지 않도록, 커밋 후에는 무조건 `LoadData()` 재호출.

### 4.6 라이트립(현재 설정 내보내기)의 의미

내보내기 결과를 그대로 다시 업로드하면 **아무 변화가 없어야 함**(멱등). 이것을 테스트의 기준점으로 삼음. 즉:

- 내보내기 시 채널코드를 채워서 출력 → 재업로드 시 전부 "수정" 판정
- 값이 동일하면 실질 변경 0건 → 요약에 `변경 없음 N건` 표시

---

## 5. 영향 범위

| 대상 | 변경 유형 |
|---|---|
| `Forms/ChannelConfigForm.cs` | 툴바 버튼 3개 추가, 커밋 후 `LoadData()` 호출 |
| 신규 `Forms/ChannelBulkImportPreviewDialog.cs` | 미리보기/분류/커밋 UI |
| 신규 `DataLoaders/ChannelBulkImportLoader.cs` | 엑셀 파싱 + 검증 → 결과 모델 반환 |
| 신규 `Exporters/ChannelTemplateExporter.cs` | 빈 양식 / 현재 설정 내보내기 |
| 신규 `Models/ChannelImportRow.cs` 등 | 파싱 결과 + 상태/사유 |
| `Database/SalesChannelRepository.cs` | 다건 Upsert를 외부 트랜잭션으로 묶을 수 있는 오버로드 필요 여부 검토 |
| `Database/DocPartyRepository.cs` | 동일 |

**스키마 변경 없음** — 기존 테이블/JSON 구조를 그대로 사용함.

---

## 6. 테스트 계획

1. 라이트립 멱등성: 내보내기 → 무수정 재업로드 → 변경 0건
2. 신규 N건 등록 시 채널코드가 `CH###`로 연속 생성되고 충돌 없음
3. 채널명 중복 파일 → 파일 단위 오류로 커밋 차단
4. 채널코드 기입했으나 미존재 → 해당 행만 오류
5. 채널코드 공란 + 채널명이 기존 DB 채널명과 일치 → 해당 행은 오류가 아니라 그 채널의 "수정"으로 분류되고, 미리보기에 어떤 기존 채널(코드)로 매칭됐는지 경고 문구가 표시됨(규칙 7-1)
6. 쿠팡그로스 채널의 `TrackingNo` 정산필드 기입 → 경고 + 그 매핑만 제외, 나머지 정상 반영
7. `설정복사원본` 지정 시 원본이 보조소스/광고비 레이아웃까지 복사 되는지(깊은 복사 확인)
7-1. `설정복사원본`이 `IsAutoOrderStandardPreset=true` 채널 → 복사본은 `false`로 리셋되고 경고 표시됨(규칙 15)
7-2. 복사원본 지정 + 발주서매핑 행 존재 → 발주서 매핑이 시트 내용으로 전면 교체되고, 정산서 매핑은 복사본 값이 유지되는지
7-3. 연쇄 복사/상호 지정 파일 → 오류 판정으로 전체 커밋 차단
8. JSON 저장 실패 시뮬레이션(파일 잠금) → 스냅샷 복원 후 DB 상태가 업로드 이전과 동일한지
9. 일괄 등록한 채널로 실제 발주파일 로드가 되는지(`OrderLoader`가 고정값/헤더행을 그대로 인식)

---

## 7. 단계별 진행 제안

| 단계 | 산출물 | 완료 기준 |
|---|---|---|
| S1 | 엑셀 형식(시트/열/유효값) | 열 구성 합의 |
| S2 | 내보내기(빈 양식 + 현재 설정) | 양식 생성·열림 확인 |
| S3 | 파싱 + 검증(UI 없이 단위 테스트) | 6장 1~7-3 통과 |
| S4 | 미리보기 다이얼로그 | 분류 결과 화면 표시 |
| S5 | 커밋 + 스냅샷/롤백 | 6장 8 통과 |
| S6 | 채널설정 창 통합 | 6장 9 통과 |

---

## 8. 확정 사항 (2026-07-31 검토 반영, 1차)

| 항목 | 결정 |
|---|---|
| v1 제외 범위(보조소스/광고비/택배사 고정값/CFS) | **제외 확정** |
| `설정복사원본` 열 | **유지 확정** — 적용 규칙은 4.1.1절 |
| 오류 행 처리 | **전체 차단 확정** — 부분 반영 옵션 없음 |
| 업로드를 통한 채널 삭제 | **미지원 확정** — 업로드에 없는 채널은 무시 |

## 9. 확정 사항 (2026-07-31 검토 반영, 2차)

| 항목 | 결정 |
|---|---|
신규 행 채널명이 기존 DB의 다른 채널(다른 코드)과 중복되는 경우 | **경고로 완화 확정(오류 아님)** — 규칙 7-1. §4.3의 "채널명 일치→수정" 분류와 모순되지 않도록, 코드 없이 이름만으로 기존 채널에 매칭된 행은 미리보기에서 어떤 채널이 수정되는지 항상 밝혀 사용자가 오타를 직접 확인하게 한다 |
| `설정복사원본`이 `IsAutoOrderStandardPreset=true` 채널인 경우 | **복사본은 자동으로 `false`로 리셋 확정**(경고 표시) — 규칙 15 신설. 근거: 이 플래그는 전체 채널 중 1개만 허용되는데 이를 강제하는 코드가 없어, 복사로 중복 생성되면 자동발주 파싱이 조용히 엉뚱한 채널로 튈 위험 |
| 시트4(`거래처정보`)에 없는 `DocParty.ProfileName` | **채널명으로 자동 세팅 확정** — 기존 화면 저장 로직과 동일 동작 명시 |
